/**
 * Arrival Phase (3 min) — Solo settling into the space.
 * Only the local presence is visible.
 * Environment responds to flight/movement.
 * Solo ambient generative audio.
 */
export class ArrivalPhase {
  enter(): void {
    // TODO: Hide all remote presences
    // TODO: Enable flight/environment response
  }

  update(_delta: number): void {}

  exit(): void {}
}

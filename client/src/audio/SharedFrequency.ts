/**
 * Merged state sound generation.
 * When two presences fully merge, a shared frequency emerges
 * that neither produces alone.
 *
 * TODO: Implement
 */
export class SharedFrequency {
  update(_delta: number): void {}
  dispose(): void {}
}

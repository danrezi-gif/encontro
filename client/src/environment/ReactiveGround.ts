/**
 * Ground that responds to presence proximity.
 * Bioluminescent glow intensifies near participants.
 *
 * TODO: Implement with custom shader
 * - Pass presence positions as uniforms
 * - Radial glow falloff around each presence
 * - Color influenced by presence color
 * - Phase-responsive base glow level
 */
export class ReactiveGround {
  update(_delta: number): void {}
  dispose(): void {}
}
